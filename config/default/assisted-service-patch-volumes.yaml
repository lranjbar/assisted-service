apiVersion: apps/v1
kind: Deployment
metadata:
  name: assisted-service
  namespace: assisted-installer
spec:
  template:
    metadata:
      labels:
        app: assisted-service
    spec:
      containers:
        volumeMounts:
        - mountPath: /data
          name: s3-filesystem
      volumes:
      - name: s3-filesystem